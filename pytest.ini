[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    --strict-markers
    --cov=basilisk_simulation
    --cov=src/input_shaping
    --cov-report=term-missing
    --cov-fail-under=80
